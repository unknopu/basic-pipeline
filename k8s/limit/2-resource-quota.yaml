apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
spec:
  hard:
    requests.cpu: "400m"
    requests.memory: "100Mi"
    limits.cpu: "750m"
    limits.memory: "500Mi"
    pods: "3"
    services: "1"
    services.loadbalancers: "0"
    persistentvolumeclaims: "0"
